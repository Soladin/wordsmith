<!DOCTYPE html>
<html>
<head>
<h1><%= @wordset.name %></h1>
</head>
<body>
<h5>From: <%= @wordset.url %></h5>


<!-- <p> hopefully will get rid of the whole 'Edit' part and will just render the form in here (show) or be able to edit with javascript.</p> -->
<%= link_to "Edit", edit_wordset_path(@wordset) %>
<%= link_to "Delete", wordset_path(@wordset), method: :delete, confirm: "Are you sure you want to delete this WordSet?" %>

  <div class="container-fluid">
  <div class="row">
    <div class="span6">
      <h2>Terms</h2>
      <li>
        <% @words.each do |word| %>
          <li><%= word.term %></li>
        <% end %>
      </li>
    </div>
    <div class="span6">
      <h2>Definitions</h2>
      <li>
        <% @words.each do |word| %>
          <li><%= word.definition %></li>
        <% end %>
      </li>
    </div>
  </div>
</div>
<%= link_to "Add New Word", new_wordset_word_path(@wordset) %>
</body>
</html>